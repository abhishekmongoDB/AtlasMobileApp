
/**
 * default language used the app is english
 */
const DEFAULT_LANGUAGE: string = 'en';
export default DEFAULT_LANGUAGE;

/**
 * list of languages taht can be used the app
 */
export const LanguagesList: { [key: string]: string } = {
  English: 'en',
  Hindi: 'hi',
};
